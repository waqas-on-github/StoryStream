// import DOMPurify from "dompurify";

// export const sanitizeInput = (inputValues: unknown) => {
//   // Sanitize the entire input object
//   // const sanitizedInput = DOMPurify.sanitize(inputValues);

//   return sanitizedInput;
// };
